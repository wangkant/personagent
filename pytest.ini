[pytest]
testpaths = tests
# The suites import the application the way it is deployed — from a checkout,
# with `persona_agent`, `main` and `quickstart` at the repository root and the
# CLI tools under `tools/`. Naming both roots here is what replaced the
# `sys.path.insert` line every suite used to open with.
pythonpath = . tools
