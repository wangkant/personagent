# The Satori side: websocket, IDENTIFY, heartbeat, resume, message elements and
# the HTTP API. The connector reads its models' raw payload (_raw_data) for
# message.quote and the channel type, so stay on the minor line it was checked
# against.
satori-python>=1.3.9,<1.4
# The agent side, through integrations/sdk/personagent_connector.py.
httpx>=0.27,<1
# Reads the optional settings file.
python-dotenv>=1.0,<2
